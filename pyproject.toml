[tool.poetry]
name = "airflow_check"
version = "0.1.0"
description = ""
authors = ["Chad Upjohn <cupjohn@condati.com>"]

[tool.poetry.dependencies]
python = "^3.6"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
